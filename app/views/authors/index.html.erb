<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title></title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
	<%content_for :header do%>
		<h2> hello...</h2>
	<% end %>
	<div>
		<h1>Articles</h1>
	</div>
	<ul>
	  <% @authors.each do |author| %>
	    <li>
	      <h2><%= author.name %></h2>
	    </li>

	    <li>
	    	<div class="description">
		    	<%= author.city %>
		    	<% if can? :read, author %>
		    		<%= link_to "Open Author profile", author, class: "btn btn-primary"%>
		    	<% end %>
	      </div>
	    </li> <br/>
	  <% end %>
	</ul>
	<section class="pagination-section">
		
		<div class="row pagination-row">
			<div class="col-lg-12 col-md-8 col-sm-7 col-xs-6 padding-zero Page navigation">
				<ul class="pagination">
					<li class="disabled"> 
						<%= will_paginate(@authors,:previous_label => '&laquo;', :next_label => "&raquo;") %>
					</li>
				</ul>
			</div>
		</div>
		
		<div class="row">
			<div class="col-lg-12 col-md-4 col-sm-5 col-xs-6 pagination-info">
				<span class="pull-right"><%= page_entries_info(@articles) %></span>
			</div>
		</div>

	</section>
	<%= link_to "New Author", new_author_path, class: "btn btn-primary" %>
</body>
</html>
