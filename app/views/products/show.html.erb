<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title></title>
</head>
<body style="background: white;">
	
	<h1><%= @product.name %></h1>
	

	<p><%= image_tag(@product.image, size: "200" )%></p>
	<p><b>Name:</b> <%= @product.name %></p>
	<p><b>Description:</b> <%= @product.description %></p>
	<p><b>Price:</b> Rs.<%= @product.price %></p>

	
		<%= link_to "Edit", edit_product_path(@product), class: "btn btn-primary" %>
	
		<%= link_to "Delete", product_path(@product), data: {turbo_method: :delete,
	turbo_confirm: "Are you sure?"}, class: "btn btn-primary" %>
	</br>
		<%= link_to "Back", :back, class: "btn btn-primary" %>
	</br>
		<%#= link_to "Buy", checkout_path(product_id: 1), method: :post, class: "btn btn-danger" %>
		<%= form_with url: url_for(controller: 'checkouts', action: 'create', product_id: @product.id), method: :post do |form| %>
  		<%= form.submit "Buy", class: "btn btn-danger", data: { turbo: false } %>
		<% end %>
</body>
</html>
